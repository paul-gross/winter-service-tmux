"""Stdlib-only Python orchestrator for winter-service-tmux.

Replaces the bash up/down/status/restart scripts behind a name-addressed
``orchestrate_services`` entrypoint, consuming the service_manifest reader.
"""
